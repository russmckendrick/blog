---
// This file makes embed components available globally
import { YouTube } from '@astro-community/astro-embed-youtube';
import { LinkPreview } from '@astro-community/astro-embed-link-preview';

// Make components available to the global scope
(globalThis as Record<string, unknown>).YouTube = YouTube;
(globalThis as Record<string, unknown>).LinkPreview = LinkPreview;
---

<!-- This component is imported to make YouTube and LinkPreview available globally -->
<script is:inline>
  // Components are now globally available
</script>