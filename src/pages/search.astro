---
import BaseLayout from '../layouts/BaseLayout.astro';
import PagefindSearch from 'astro-pagefind/components/Search';

const isDev = import.meta.env.DEV;
---

<BaseLayout title="Search" description="Search Russ.Cloud blog posts and content">
	<div class="max-w-[728px] mx-auto px-6">
		<section class="pt-4 pb-10 border-b" style="border-color: var(--color-outline-variant);">
			<h1 data-entrance>
				Search
			</h1>
			<p class="mt-3 text-lg text-on-surface-variant leading-normal" data-entrance>
				Find articles, tutorials, and insights
			</p>
		</section>

		{isDev && (
			<div class="callout callout-warning mt-6">
				<p class="callout-heading">Development Mode</p>
				<p>
					Search requires a production build to index content. Run{' '}
					<code>npm run build</code>{' '}
					and{' '}
					<code>npm run preview</code>{' '}
					to test search functionality.
				</p>
			</div>
		)}

		<section class="py-6 md:py-8">
			<PagefindSearch
				id="search"
				className="pagefind-ui"
				uiOptions={{ showImages: false, excerptLength: 15 }}
			/>
		</section>
	</div>
</BaseLayout>

<script>
	// Reinitialize Pagefind after View Transitions navigation
	function initPagefind() {
		const searchElement = document.querySelector('.pagefind-ui');
		if (searchElement && window.PagefindUI) {
			// Clear existing content
			searchElement.innerHTML = '';
			// Reinitialize
			new window.PagefindUI({
				element: searchElement,
				showImages: false,
				excerptLength: 15
			});
		}
		focusInput();
	}

	// The page is the no-JS fallback and deep link for the masthead search
	// sheet, so drop the reader straight into the input. PagefindUI renders
	// it asynchronously — poll briefly.
	function focusInput(attempts = 30) {
		const input = document.querySelector<HTMLInputElement>('#search .pagefind-ui__search-input');
		if (input) {
			input.focus();
		} else if (attempts > 0) {
			requestAnimationFrame(() => focusInput(attempts - 1));
		}
	}

	// Initialize on first load
	initPagefind();

	// Re-initialize after View Transitions navigation
	document.addEventListener('astro:page-load', initPagefind);
</script>

<!-- Pagefind UI theming lives in global.css, shared with the masthead search sheet. -->
