# Use legacy peer deps for compatibility
legacy-peer-deps=true

# Engine strict false for CI/CD compatibility
engine-strict=false

# Don't fail on optional dependencies (pagefind binaries)
optional=true
