# Use legacy peer deps for compatibility
legacy-peer-deps=true

# Prevent optional dependencies issues on different platforms
optional=true

# Engine strict false for CI/CD compatibility
engine-strict=false
